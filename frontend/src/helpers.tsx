// const { SERVER_PROTOCOL, SERVER_URL, SERVER_PORT } = process.env;

// export const heroesUrl = `${SERVER_PROTOCOL}://${SERVER_URL}:${SERVER_PORT}/api/heroes`;
export const heroesUrl = `http://localhost:3000/api/heroes`;